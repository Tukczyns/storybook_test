import { create } from '@storybook/theming';
import logo from '../src/images/storybook_logo.png';

export default create({
    base: 'light',
    brandTitle: 'Komunikuje components',
    brandUrl: 'https://johncube.pl',
    brandImage: logo,
});